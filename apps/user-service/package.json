{
  "name": "user-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/domain/main/server.ts",
    "generate": "prisma generate --config prisma.config.ts",
    "build": "tsc"
  }
}
